# RPC endpoint for the Sepolia testnet (e.g. Alchemy or Infura URL)
SEPOLIA_RPC_URL=

# Deployer private key (NEVER commit the real .env). Use a throwaway test key.
PRIVATE_KEY=

# Etherscan API key for contract verification
ETHERSCAN_API_KEY=
